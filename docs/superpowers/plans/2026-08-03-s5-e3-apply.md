# S5 — E3 Bundle Enrollment screens (plan)

Design: [2026-08-03-s5-e3-apply-design.md](../specs/2026-08-03-s5-e3-apply-design.md).
Branch: `feature/s5-e3-apply` off `stage` @ `dee8a8f`. TDD throughout (pure render/logic first).

## Tasks

1. **S5.1 — landing apply CTA (#34).** Add a window-gated `#/events/:id/apply` CTA to
   `renderLanding`. Test: CTA present when `state==='Open'`, absent when `'Closed'`.
2. **S5.3 — participants confirmed-only (#36).** Filter `renderParticipants` to
   `status==='Confirmed'`. Test: a mixed Applied/Confirmed list renders only the Confirmed row.
   (Bundled with S5.1 — both touch `landing.ts`/`participants.ts` + `landing.test.ts`.)
3. **S5.2 — coach apply form (#35).** New `views/apply.ts` (`openCategories`, `buildApplyInput`,
   `renderApply`) + `#/events/:id/apply` route in `main.ts` wiring submit → `o5.applyRegistration`.
   Tests: open-category filtering, form fields + submit button, no-token notice, trimmed DTO.
4. **S5.4 — acceptance E2E (#37).** `test/e2e/s5-bundle-enrollment.e2e.test.ts`: skip-gated full
   loop create → open → apply → inbox → confirm → fee paid → public confirmed participant.
   Redeploy `stg` (S4 `o12` fee-read) if needed, then run `API_BASE_URL=<stg> npm run test:e2e`.

## Verification

- `npm test` (unit) · `npm run lint` · `npm run build` all green.
- `API_BASE_URL=<stg-api> npm run test:e2e` green against real AWS (the S5.4 collaudo).

## Commits (feature/s5-e3-apply)

- `feat(s5.1,s5.3)` — landing apply CTA (window-gated) + participants confirmed-only guard.
- `feat(s5.2)` — E3 coach apply form via magic-link (`o5.applyRegistration`).
- `test(s5.4)` — E1<->E3 bundle-enrollment full-loop acceptance E2E (skip-gated).
- `docs(s5)` — design + plan.
