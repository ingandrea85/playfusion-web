// TEMPORARY S0.1 scaffold. This package only exists so the 'libs' layer is a
// non-empty, buildable/testable Nx project. Delete it when the real libs
// content lands (services=S0.2, libs=S0.5, infra=S0.6+, apps=S6+).
export const placeholder = 'lib' as const
