import type { Screen } from '../view.js'

/** Placeholder — replaced by Task 6 (S4.6/S4.7 partecipanti tab). */
export const participantsScreen: Screen<unknown> = {
  load: async () => ({}),
  render: () => '<main class="pf-container"><div class="pf-card pf-muted">In arrivo (S4).</div></main>',
}
