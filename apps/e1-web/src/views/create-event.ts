import type { Screen } from '../view.js'

/** Placeholder — replaced by Task 4 (S4.1/S4.2 create-event flow). */
export const createEventScreen: Screen<unknown> = {
  load: async () => ({}),
  render: () => '<main class="pf-container"><div class="pf-card pf-muted">In arrivo (S4).</div></main>',
}
