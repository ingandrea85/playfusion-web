{
  "name": "@playfusion/infra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy --all",
    "cdk": "cdk",
    "synth:local": "cdklocal synth -c env=local",
    "deploy:local": "cdklocal deploy --all -c env=local --require-approval never",
    "lint": "eslint ."
  },
  "dependencies": {
    "aws-cdk-lib": "^2.240.0",
    "constructs": "^10.5.0"
  },
  "devDependencies": {
    "aws-cdk": "^2.1000.0",
    "aws-cdk-local": "^2.19.0",
    "tsx": "^4.16.0"
  },
  "nx": {
    "tags": ["scope:infra", "type:infra"]
  }
}
